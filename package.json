{
  "name": "leadership-legacy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "sitemap": "node scripts/build-sitemap.js",
    "deploy": "npm run build && wrangler deploy",
    "cf:preview": "npm run build && wrangler dev",
    "cf:tunnel:help": "node scripts/print-cloudflare-tunnel-help.js",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:report": "playwright show-report",
    "r2:publish": "node scripts/publish-dist-to-r2.mjs",
    "r2:prune": "node scripts/prune-r2-deployments.mjs",
    "deploy:full": "npm run build && npm run r2:publish && npm run r2:prune && wrangler deploy"
  },
  "dependencies": {
    "@monaco-editor/react": "^4.7.0",
    "@vitejs/plugin-react": "latest",
    "@xterm/addon-fit": "latest",
    "@xterm/xterm": "latest",
    "lucide-react": "latest",
    "monaco-editor": "^0.53.0",
    "react": "latest",
    "react-dom": "latest",
    "react-router-dom": "latest",
    "vite": "latest"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "playwright": "^1.59.1"
  }
}
